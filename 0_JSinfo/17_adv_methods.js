// forEach(call back): it is used to iterate each and every element of the array and pass ele



// map(): - it is also used similarly as forEach but it is return values for every element of given array
//Syntax: 
// 


// filter() :- it is also used similarly as forEach but it is return only the element with return condition 'true'.



// include(): - include is used to check whether given elements is existed in the array or not. if it existed, it will return true else false.

// let x = arr.include("prasad")

// find(callback) : - it is used to check the first elements that satisfied the return condition and return it  